import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pet Feeding Guide - Expert Nutrition Advice for Dogs, Cats, Birds & More",
  description: "Comprehensive pet feeding guides and nutrition advice for dogs, cats, birds, and other pets. Learn proper feeding schedules, portion sizes, and dietary requirements.",
  keywords: "pet feeding, dog nutrition, cat food, bird diet, pet care, animal nutrition",
  openGraph: {
    title: "Pet Feeding Guide",
    description: "Expert nutrition advice for your pets",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-gray-50">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
